version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - GATE_PASSWORD=${GATE_PASSWORD:-}
      - DB_PATH=/data/formfiller.db
    volumes:
      - formfiller-data:/data

volumes:
  formfiller-data:
